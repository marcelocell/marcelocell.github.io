console.log('This would be the main JS file.');

function confirmarSelecao(musica, artista, codigo) {
    if (confirm("Selecionar a música \"" + musica + "\" de " + artista) == true) {
        alert("Música selecionada! (SIMULAÇÂO)");
    } 
}
